import { COLORS, FONT_SIZE } from "../../constants/theme"

export const styles = {
    icone: {
        width: 300,
        height: 150
    },
    banner: {
        padding: 10,
        marginTop: 20,
        marginBottom: 20
    },
    descricao: {
        fontSize: FONT_SIZE.xsm,
        color: COLORS.dark_gray,
        textAlign: "center"
    }
}