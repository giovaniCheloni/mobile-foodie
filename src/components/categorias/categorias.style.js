import { COLORS, FONT_SIZE } from "../../constants/theme"

export const styles = {
    icone: {
        width: 80,
        height: 80
    },
    categoria: {
        paddingRight: 10,
        padding: 10
    },
    descricao: {
        fontSize: FONT_SIZE.xsm,
        color: COLORS.dark_gray,
        textAlign: "center"
    }
}